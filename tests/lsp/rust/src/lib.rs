/// Add returns the sum of two integers.
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}

/// Compute the ratio of two integers as a float.
pub fn compute_ratio(a: i32, b: i32) -> f64 {
    a as f64 / b as f64
}
